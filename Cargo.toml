[package]
name = "nano-vllm-rs"
version = "0.0.1"
edition = "2024"
license = "MIT"
description = "A tiny LLM inference engine built from scratch in Rust, one day at a time"

[dependencies]
# `=` pins the exact version: every learner builds byte-identical dependencies.
candle-core = "=0.11.0"
